{
  "name": "sole-resolver",
  "version": "1.0.0",
  "description": "Universal shoe style code & SKU resolver API",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "prestart": "node -e \"if (process.env.RENDER) { const fs=require('fs'); const cp=require('child_process'); fs.rmSync('node_modules', { recursive: true, force: true }); cp.execSync('npm ci --ignore-scripts --no-fund --no-audit', { stdio: 'inherit' }); }\"",
    "start": "node src/index.js"
  },
  "dependencies": {
    "fastify": "^4.25.2",
    "@fastify/cors": "^8.5.0",
    "@fastify/rate-limit": "^9.1.0",
    "dotenv": "^16.4.1"
  },
  "engines": {
    "node": "20.x"
  }
}
